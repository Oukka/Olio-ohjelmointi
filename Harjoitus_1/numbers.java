// Ask for positive integers until zero or negative number input
// Hold the statistics for total inputted numbers, the largest and the smallest given number
// Print total numbers, the mean of the numbers, largest and smallest number
Integer Integer .MAX_VALUE
Integer.MIN_VALUE.
import java.io.Console; /*yksittäisten rivien lukeminen*/

class Numbers{
    private static final Console console  = System.console();
    public static void main(String args[])
    {
        while (true){
            while (true){
                System.out.println("Enter a positive integer>");
                int integer = console.readLine();
                try
                {
                    integer=Integer.parseInt(obj); //use your variable or object in place of obj
                    System.out.println(int + " is a integer number.");
                    break;
                }

                catch (NumberFormatException e)
                {
                    continue;
                }
            }
            if (integer<=0){
                /* print stats*/
                }
    
    
    
}    